import start

function(HTTP) {
    connect(HTTP) {
        
    }
    exit::();
}

Class HTTP {
    give()::{
        Method::(HTTP) {
            
        }
    }
    exit::();
}

import exit::end::();