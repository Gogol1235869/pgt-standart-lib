#build <http.pgt>

import start 

function(POST) {
    give()::{
       from::{
        Path::("libs/internet_libs/http.pgt")

       } 
    }
    exit::();
}

Class ::(http.pgt) {
    
    exit::();
}

import exit::end::();